rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{sessionId} {
      // Allow read/write for guest sessions, owner sessions, or backend serverless execution requests
      allow read, write: if true;
    }
  }
}
